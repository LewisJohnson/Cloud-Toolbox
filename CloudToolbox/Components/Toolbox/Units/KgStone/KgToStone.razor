@attribute [Route(Routes.UNITS_KG_TO_STONE)]

@namespace CloudToolbox.Components.Toolbox.Units

@using System.Text;
@using CloudToolbox.Data;
@using CloudToolbox.Common.Routes;
@using CloudToolbox.Components.Shared;

<ToolboxHeader Title="kg to stone"
               Description="Converts kg to stone (st)" />

<InputOutput onChangeEvent="@OnChange" />

@code {
    public async Task<string> OnChange(string input)
    {
        if (input != null)
        {
            if (double.TryParse(input, out double num))
            {
                return (num / 6.35).ToString("#.##");
            } else
            {
                return "Invalid input";
            }
        }

        return string.Empty;
    }
}
