@attribute [Route(Routes.UNITS_KMH_TO_MPH)]

@namespace CloudToolbox.Components.Toolbox.Units

@using System.Text;
@using CloudToolbox.Data;
@using CloudToolbox.Common.Routes;
@using CloudToolbox.Components.Toolbox.Shared;

<ToolboxHeader Title="km/h to mi/h"
               Description="Converts kph to mph
mph = mph, m.p.h., MPH, or mi/h
kph = km/h or kmph" />

<InputOutput onChangeEvent="@OnChange" />

@code {
    public async Task<string> OnChange(string input)
    {
        if (input != null)
        {
            if (double.TryParse(input, out double num))
            {
                return (num * 1.609344).ToString("#.##");
            } else
            {
                return "Invalid input";
            }
        }

        return string.Empty;
    }
}
