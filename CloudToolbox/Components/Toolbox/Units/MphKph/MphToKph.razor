@namespace CloudToolbox.Components.Toolbox
@page "/Toolbox/Units/Mph-To-Kph"

@using System.Text;
@using CloudToolbox.Components.Toolbox.Shared;
@using CloudToolbox.Data;

<ToolboxHeader Title="mi/h To km/h"
               Description="Converts mph to kph. 
/n mph = mph, m.p.h., MPH, or mi/h
/n kph = km/h or kmph" />

<InputOutput onChangeEvent="@OnChange" />

@code {
    public async Task<string> OnChange(string input)
    {
        if (input != null)
        {
            if (decimal.TryParse(input, out num))
            {
                return num / 1.609344;
            } else
            {
                return "Invalid input";
            }
        }

        return string.Empty;
    }
}
