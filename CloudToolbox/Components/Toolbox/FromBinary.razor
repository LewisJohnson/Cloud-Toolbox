@page "/Toolbox/From-Binary"
@using System.Text;
@using CloudToolbox.Toolbox.Shared;
@using CloudToolbox.Data;

<PageTitle>@Constants.TITLE_PREFIX From Binary</PageTitle>

<h1>From Binary</h1>

<InputOutput onChangeEvent="@OnChange" />

@code {
    public async Task<string> OnChange(string input)
    {
        if (input != null)
        {
            try
            {
                var str = input.Replace(" ", "");

                StringBuilder sb = new StringBuilder();
                while (str.Length > 0)
                {
                    string block = str.Substring(0, 8);
                    str = str.Substring(8);
                    int value = 0;
                    foreach (int x in block)
                    {
                        int temp = x - 48;
                        value = (value << 1) | temp;
                    }
                    sb.Append(Convert.ToChar(value));
                }

                return sb.ToString();
            } catch (Exception _)
            {
                return "Invalid input";
            }
        }

        return string.Empty;
    }

}
