@attribute [Route(Routes.AREA_TEXT)]

@namespace CloudToolbox.Components.Collections
@inject CalculatorCollectionService CollectionService

@if (CalcCollection != null)
{
    <CalculatorCollectionDisplay Collection="CalcCollection">
        <Description>
            HavE YoUR LeTTers In a juMBle?<br />
            Well one of our @CalcCollection.Calculators.Count free text converters can help you fix that.
        </Description>
    </CalculatorCollectionDisplay>
}

@code {
    CalculatorCollection? CalcCollection;

    protected override void OnParametersSet()
    {
        CalcCollection = CollectionService.GetCollectionOfType(CalculatorAreaTypes.Text);
    }
}
