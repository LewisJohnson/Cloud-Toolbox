@attribute [Route(Routes.AREA_DEVELOPER)]

@namespace CloudToolbox.Components.Toolbox.Areas

@using System.Text;
@using CloudToolbox.Data;
@using CloudToolbox.Common.Routes;
@using CloudToolbox.Components.Toolbox.Shared;

<h1>Developer Calculators</h1>

<div class="row">
    @foreach (var calc in Areas.Developer.Calculators.OrderBy(x => x.DisplayName))
    {
        <AreaButton DisplayName="@calc.DisplayName" Link="@calc.Uri"></AreaButton>
    }
</div>